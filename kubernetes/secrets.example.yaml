# Template only. The real secret is NOT committed (see .gitignore).
# Create it with:
#   kubectl -n attendance create secret generic attendance-secrets \
#     --from-literal=POSTGRES_PASSWORD='<strong-password>' \
#     --from-literal=API_KEY='<api-key>'
apiVersion: v1
kind: Secret
metadata:
  name: attendance-secrets
  namespace: attendance
type: Opaque
stringData:
  POSTGRES_PASSWORD: "replace-me"
  API_KEY: "replace-me"
